import { Component, computed, input, model, OnInit } from '@angular/core';
import { NoteDto } from '../../../shared/models/note-dto.model';

@Component({
  selector: 'app-note',
  imports: [],
  templateUrl: './note.html',
  styleUrl: './note.css'
})
export class Note implements OnInit{

  ngOnInit(): void {
    
  }
}
